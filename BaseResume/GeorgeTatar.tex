\documentclass[11pt]{article}
\usepackage[letterpaper,margin=0.5in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{tgheros}
\renewcommand{\familydefault}{\sfdefault}
\usepackage{microtype}
\usepackage[svgnames]{xcolor}
\usepackage[hidelinks]{hyperref}
\usepackage{tabularx}
\usepackage{array}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{setspace}
\usepackage{textcomp}
\usepackage{ragged2e}
\usepackage{etoolbox}

\newcommand{\newflag}[2]{%
  \newtoggle{#1}%
  \ifstrequal{#2}{true}{\toggletrue{#1}}{\togglefalse{#1}}%
}
\newcommand{\IF}[2]{\iftoggle{#1}{#2}{}}
\newcommand{\IFNOT}[2]{\iftoggle{#1}{}{#2}}
\newcommand{\IFELSE}[3]{\iftoggle{#1}{#2}{#3}}

% Flags to include or exclude parts of the resume
\newflag{TAGLINE}{true}   % if we want a tagline on the resume
\newflag{SUMMARY}{true}   % if we want a summary block on the resume
\newflag{SKILLS}{true}    % if we want a skills block on the resume

\newflag{MF}{true}        % if we are targetting mainframe modernization jobs
\newflag{EMBEDDED}{false}  % if we are targetting firmware/embedded jobs
\newflag{COMPILER}{true}  % if we are targetting compiler-related jobs

\pagenumbering{gobble}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\setstretch{1.05}
\definecolor{accent}{HTML}{0F2740}
\definecolor{textgray}{HTML}{222222}
\definecolor{muted}{HTML}{4B5563}

\titleformat{\section}
  {\Large\bfseries\color{accent}}
  {}
  {0pt}
  {}
  [\vspace{3pt}{\color{accent}\titlerule[0.8pt]}]
\titlespacing*{\section}{0pt}{12pt}{8pt}

\setlist[itemize]{
  leftmargin=1.2em,
  itemsep=3pt,
  topsep=4pt,
  parsep=0pt,
  partopsep=0pt
}

\newcommand{\resheader}[1]{\section*{#1}\vspace{-4pt}}
\newcommand{\jobheading}[4]{%
  \vspace{6pt}\begin{tabularx}{\textwidth}{@{}X r@{}}
    {\textbf{#1}} & {\textbf{#2}} \\
    {{\normalsize #3}\hspace{0.5em}{\color{muted}·}\hspace{0.5em}{\color{muted}#4}} & {} \\
  \end{tabularx}\vspace{2pt}
}

\begin{document}

{\centering
  {\fontsize{21}{23}\selectfont\bfseries GEORGE TATAR}\\[8pt]
  \IF{TAGLINE}{{\normalsize\color{accent}Principal-level software engineer \textbar\ 25 years in distributed systems, storage, and cloud migration\\}}%
  {\normalsize\color{textgray}Needham, MA \textbar\ (617) 515-9081 \textbar\ \href{mailto:gtatar@tatar-family.com}{gtatar@tatar-family.com}}\\
\par}

\IF{SUMMARY}{%
  \resheader{SUMMARY}

  25 years building systems where data integrity and performance are non-negotiable.
  %Principal/staff IC for modernization, cloud migration, and storage internals.
  Recent work spans million-line mainframe conversion tooling at Amdocs, decomposition of a .NET monolith into $\sim$70 AWS microservices across 5 factories at Cimpress, and Actifio storage data-path design with p99 $<$ 300~\textmu s latency and $\sim$8 GB/s cross-DC replication.
}%

\IF{SKILLS}{%
  \resheader{SKILLS}

  \begin{itemize}
  \IFNOT{SUMMARY}{\item Principal/Staff IC across distributed systems, storage, cloud platforms, and legacy modernization.}%
  % need substantiation for WCF, IIS, Windows Server -- still including
  \item Languages: Java, C\#, C++, C, Python, SQL.
  \item Cloud \& backend: AWS, Kubernetes, Docker, .NET Core, Spring, PostgreSQL, SQL Server, Oracle, MariaDB, REST, OAuth 2.0, CI/CD.
  \item Specialized tech: ETL, Linux, Android, device drivers\IF{EMBEDDED}{, Bluetooth, WPF, SQLite}, SAN, iSCSI, Fibre Channel\IF{MF}{; mainframe modernization stack: COBOL, JCL, DB2, CICS}.
  \end{itemize}
}%

\resheader{EXPERIENCE}

\jobheading{AMDOCS}{2023-present}{\IFELSE{MF}{Mainframe Modernization Engineer}{Cloud Migration Engineer, Platform \& Tooling}}{Remote}
\begin{itemize}
\item Architect and implement core components of an automated mainframe-to-cloud migration platform.
\item Build code conversion tools that transform millions of lines of legacy \IF{MF}{COBOL, JCL, and CICS }applications into functionally equivalent C\# or Java systems.
\item Develop ETL tooling to migrate schemas and data from legacy databases\IF{MF}{ (DB2, ADABAS, IDMS)} to modern SQL platforms such as SQL Server, Oracle, and PostgreSQL.
\item Develop Java and C\# compatibility libraries that faithfully emulate mainframe features on Linux and Windows.
%\item Designed the containerized runtime platform for migrated workloads, packaging converted C\# and Java services with Docker and deploying them to Kubernetes on Amazon EKS.
\item C\#, Java, SQL Server, Oracle, PostgreSQL.
\end{itemize}

\jobheading{ZYRLO, LLC}{2021-2023}{Software Architect}{Sudbury, MA}
\begin{itemize}
\IFELSE{EMBEDDED}{%
\item Defined the software architecture and implemented core system components for an assistive device for visually impaired users.
\item Architected and built the software stack around the main CM4 controller running Ubuntu Core, with an SD410/Android subsystem for braille and accessibility, and ESP32 firmware.
\item Implemented the key software/hardware interfaces across the product stack, including custom protocols, Bluetooth, Android integration, and Linux drivers.
\item Worked with hardware engineers on schematic reviews, board bring-up, and hardware/software debugging, including bootloader configuration and driver integration.
}{%
\item Architected the software stack for an assistive device spanning CM4/Ubuntu Core, an SD410/Android subsystem for braille and accessibility, and ESP32 firmware.
\item Implemented the key software and hardware interfaces across the product stack, including custom protocols, Bluetooth, Android integration, and Linux drivers.
}%
\item C, C++, Python, Ubuntu Core, Android, Bluetooth, device drivers.
\end{itemize}

\jobheading{AKRUTO, INC.}{2019-2021}{Founder}{Needham, MA}
\begin{itemize}
\item Founded and shipped AkrutoSync, a commercial product syncing Microsoft Outlook contacts/calendars with smartphones.
%\item Owned end-to-end product delivery and business operations including Stripe billing, customer support, and contractor management.
\item Built an Exchange ActiveSync server inside a Windows desktop app that iOS Mail and Android's native Exchange clients connected to as if it were Exchange itself, reading the user's local Outlook via MAPI and driving stateful, bidirectional sync with change tracking and conflict resolution for contacts and calendars, all from Microsoft's protocol specifications and observed server behavior.
%\item Built and operated the customer-facing website, plus licensing, billing and auto-update services  driven by a CI/CD pipeline for automated releases.
\item C\#, .NET Framework, WPF, MAPI, Windows, SQLite.
\end{itemize}

\jobheading{CIMPRESS/VISTAPRINT}{2013-2019}{Lead Software Engineer}{Waltham, MA}
\begin{itemize}
\item Led an 8-engineer team within a 1,000-engineer org in a company-wide initiative to migrate a legacy .NET/WPF/SQL Server monolith to $\sim$70 REST/JSON microservices on AWS (.NET Core on Ubuntu, MariaDB).
\item Architected the microservices for our team's domains, running mission-critical operations across 5 global factories: consumer order processing, production scheduling, supply chain, worker shifts, and shipping.
\item Led design and implementation of AWS infrastructure (EC2, RDS, S3, CloudFront; later Docker on EKS), defined in CloudFormation and deployed via CI/CD pipelines.
\IF{EMBEDDED}{\item Integrated digital printing presses and embroidery machines via vendor protocols and custom interfaces.}%
\item C\#, .NET Core, AWS (EC2, RDS, S3, EKS), Docker, Kubernetes, MariaDB, CloudFormation, OAuth 2.0/Auth0.
\end{itemize}

\jobheading{DESKTONE, INC.}{2012-2013}{Engineering Lead}{Lexington, MA}
\begin{itemize}
\item Re-architected a single-datacenter Desktop-as-a-Service control plane into a redundant multi-DC system with paired instances of each node type per datacenter, designed to survive full-datacenter loss.
\item Designed cross-DC PostgreSQL replication, inter-service communication, and failover flows for a control plane managing virtual desktops across public/private clouds, validating the architecture via full DC-cutover exercises.
\item Java, Tomcat, Spring, Hibernate, Ubuntu Linux, PostgreSQL, Python.
\end{itemize}

\jobheading{ACTIFIO}{2009-2012}{Architect}{Waltham, MA}
\begin{itemize}
\item On the data-path team for Actifio's Copy Data Management platform, wrote and maintained the on-disk format and indexing subsystem behind dedup-backed virtual copies: copy-on-write B-trees, content-addressable dedup index, garbage collection, and crash-consistent recovery.
\item Achieved sub-300~\textmu s p99 latency while enforcing strict per-write durability for connected hosts over Fibre Channel and iSCSI.
\item Designed the wire format, send-side serialization, and receive-side ingest for cross-datacenter replication of this on-disk subsystem; the team's pipelined TCP replication path sustained $\sim$8 GB/s in production and supported offsite backup, DR, and cross-site dedup.
%\item Architect-level reviewer on adjacent data-path designs (snapshot/instant-mount, application capture).
\item C++, SUSE Linux, Fibre Channel, iSCSI, SAN.
\end{itemize}

\jobheading{APPIQ, INC. (acquired by HP in 2005)}{2001-2009}{Software Engineer, Sr Software Engineer, Principal Software Engineer}{Marlborough, MA}
\begin{itemize}
\item Built Storage Essentials' multi-vendor management framework and vendor adapters for SAN arrays and switches.
\item Selected for a small Advanced Development team chartered with multi-quarter architectural initiatives; shipped a platform re-architecture delivering horizontal scale-out, SAN-fault resilience, and a new management UI.
\item Led Sustaining Engineering for 2 years across 100+ enterprise deployments; cut recurring P1 escalations $\sim$40\%.
\item Java, JBoss, Tomcat, Spring, Windows, SunOS, Red Hat Linux, Oracle.
\end{itemize}

\resheader{EDUCATION}

\jobheading{HARVARD UNIVERSITY EXTENSION SCHOOL}{2005}{Master of Liberal Arts, Information Technology}{Cambridge, MA}

Master's thesis: C compiler front end and code generator able to round-trip the full Apache web server source (C $\to$ internal representation $\to$ C), then compile and run it.

\end{document}
