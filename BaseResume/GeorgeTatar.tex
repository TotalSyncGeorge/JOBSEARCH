\documentclass[11pt]{article}
\usepackage[letterpaper,margin=0.68in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{tgheros}
\renewcommand{\familydefault}{\sfdefault}
\usepackage{microtype}
\usepackage[svgnames]{xcolor}
\usepackage[hidelinks]{hyperref}
\usepackage{tabularx}
\usepackage{array}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{setspace}
\usepackage{ragged2e}
\usepackage{etoolbox}

\newcommand{\newflag}[2]{%
  \newtoggle{#1}%
  \ifstrequal{#2}{true}{\toggletrue{#1}}{\togglefalse{#1}}%
}
\newcommand{\IF}[2]{\iftoggle{#1}{#2}{}}
\newcommand{\IFNOT}[2]{\iftoggle{#1}{}{#2}}
\newcommand{\IFELSE}[3]{\iftoggle{#1}{#2}{#3}}

% Flags to include or exclude parts of the resume
\newflag{TAGLINE}{true}   % if we want a tagline on the resume
\newflag{SUMMARY}{true}   % if we want a summary block on the resume
\newflag{SKILLS}{true}    % if we want a skills block on the resume

\newflag{MF}{true}        % if we are targetting mainframe modernization jobs
\newflag{EMBEDDED}{true}  % if we are targetting firmware/embedded jobs
\newflag{COMPILER}{true}  % if we are targetting compiler-related jobs

\pagenumbering{gobble}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\setstretch{1.05}
\definecolor{accent}{HTML}{0F2740}
\definecolor{textgray}{HTML}{222222}
\definecolor{muted}{HTML}{4B5563}

\titleformat{\section}
  {\Large\bfseries\color{accent}}
  {}
  {0pt}
  {}
  [\vspace{3pt}{\color{accent}\titlerule[0.8pt]}]
\titlespacing*{\section}{0pt}{12pt}{8pt}

\setlist[itemize]{
  leftmargin=1.2em,
  itemsep=3pt,
  topsep=4pt,
  parsep=0pt,
  partopsep=0pt
}

\newcommand{\resheader}[1]{\section*{#1}\vspace{-4pt}}
\newcommand{\jobheading}[4]{%
  \vspace{6pt}\begin{tabularx}{\textwidth}{@{}X r@{}}
    {\textbf{#1}} & {\textbf{#2}} \\
    {{\normalsize #3}\hspace{0.5em}{\color{muted}·}\hspace{0.5em}{\color{muted}#4}} & {} \\
  \end{tabularx}\vspace{2pt}
}

\begin{document}

{\centering
  {\fontsize{21}{23}\selectfont\bfseries GEORGE TATAR}\\[8pt]
  \IF{TAGLINE}{{\normalsize\color{accent}Principal-level software engineer \textbar\ 25 years in distributed systems, storage, and cloud migration\\}}%
  {\normalsize\color{textgray}Needham, MA \textbar\ (617) 515-9081 \textbar\ \href{mailto:gtatar@tatar-family.com}{gtatar@tatar-family.com}}\\
\par}

\IF{SUMMARY}{%
  \resheader{SUMMARY}

  Principal/staff IC: correctness- and performance-critical systems. Recent focus: codegen and migration tooling at mainframe scale (Amdocs); org-wide cloud decomposition and factory-scale microservices (Cimpress); storage data paths, b-trees, and cross-DC replication (Actifio).
}%

\IF{SKILLS}{%
  \resheader{SKILLS}

  \begin{itemize}
  \IFNOT{SUMMARY}{\item Principal/Staff IC across distributed systems, storage, cloud platforms, and legacy modernization.}%
  \item Languages \& runtimes: Java, C\#, C++, C, Python; JS/HTML/CSS; .NET Core/Framework, Spring, Tomcat, Hibernate; performance- and integrity-critical services.
  \item Cloud \& data: AWS (EC2, RDS, S3, CloudFront, CloudFormation, EKS), Kubernetes, Docker, REST, OAuth~2.0; PostgreSQL, SQL Server, Oracle, MariaDB; ETL and schema migration.
  \item Systems breadth: Linux, Windows Server; embedded (firmware, drivers, Android, Bluetooth); \IF{MF}{COBOL, JCL, DB2, CICS;} SAN, iSCSI, FC; SQLite, WPF, WCF, IIS; SOAP/XML/JSON/YAML; CI/CD.
  \end{itemize}
}%

\resheader{EXPERIENCE}

\jobheading{AMDOCS}{2023-present}{\IFELSE{MF}{Mainframe Modernization Engineer}{Cloud Migration Engineer, Platform \& Tooling}}{Remote}
\begin{itemize}
\item Own core components of an automated mainframe-to-cloud migration platform: codegen and transforms that convert millions of lines of legacy \IF{MF}{COBOL/JCL/CICS }workloads into C\#/Java, plus ETL from legacy stores\IF{MF}{ (DB2, ADABAS, IDMS)} to SQL Server, Oracle, and PostgreSQL.
\item Deliver Java and C\# compatibility libraries that emulate mainframe semantics on Linux and Windows for converted applications.
\item C\#, Java, SQL Server, Oracle, PostgreSQL.
\end{itemize}

\jobheading{ZYRLO}{2021-2023}{Software Architect}{Sudbury, MA}
\begin{itemize}
\item Architected an assistive-device product: CM4 (Ubuntu Core), Android (SD410) for braille/accessibility, ESP32 firmware; owned cross-stack interfaces (custom protocols, Bluetooth, drivers, bootloader/board bring-up with hardware).
\item C, C++, Python, Ubuntu Core, Android, Bluetooth.
\end{itemize}

\jobheading{AKRUTO, INC}{2019-2021}{Founder}{Needham, MA}
\begin{itemize}
\item Founded AkrutoSync: Exchange-based Outlook sync to mobile devices; built protocol handling (SOAP/XML, OAuth~2.0/M365) and the C\#/WPF engine with SQLite.
\item Shipped the full product surface: website (JavaScript/HTML/CSS), licensing and auto-update services, CI/CD releases, Stripe billing, support, and contractor coordination.
\end{itemize}

\jobheading{CIMPRESS/VISTAPRINT}{2013-2019}{Lead Software Engineer}{Waltham, MA}
\begin{itemize}
\item Led 8 engineers in a $\sim$1{,}000-engineer organization executing a company-wide decomposition of a .NET/WPF/SQL Server monolith into REST microservices on AWS (.NET Core on Ubuntu, MariaDB): phased decoupling, CI/CD, YAML CloudFormation (EC2, RDS, S3, CloudFront); later Dockerized services and moved production to EKS with rolling updates and autoscaling across 5 global factories.
\item Architected domain microservices for orders, production scheduling, supply chain, shifts, and shipping; integrated new manufacturing equipment (digital presses, embroidery).
\item Rolled out OAuth~2.0/Auth0 across the new architecture for service-to-service and user authZ.
\end{itemize}

\jobheading{DESKTONE, INC}{2012-2013}{Engineering Lead}{Lexington, MA}
\begin{itemize}
\item Engineering lead for multi-tenant Desktop-as-a-Service; drove single-DC $\rightarrow$ multi-DC architecture with PostgreSQL replication, cross-DC links, and automated failover/failback.
\item Specified redundant topologies (paired node roles per DC) so failures could be absorbed without full-site loss.
\item Java, Tomcat, Spring, Hibernate, Ubuntu Linux, PostgreSQL, Python.
\end{itemize}

\jobheading{ACTIFIO, INC}{2009-2012}{Architect}{Waltham, MA}
\begin{itemize}
\item Architected storage-virtualization data-path components: b-trees, indexing, garbage collection, on-disk layout, backup/restore---block-IO path work where every IO had to be correct and meet sub-millisecond latency budgets.
\item Designed TCP-based cross-datacenter replication tuned for multi-GB/s sustained throughput on deployed hardware.
\item C++, SUSE Linux, Fibre Channel, iSCSI, SAN.
\end{itemize}

\jobheading{APPIQ, INC. (acquired by HP in 2005)}{2001-2009}{Software Engineer, Sr Software Engineer, Principal Software Engineer}{Marlborough, MA}
\begin{itemize}
\item 8 years on HP/AppIQ Storage Essentials: advanced development toward scale-out deployment and SAN fault tolerance; built the provider framework for multi-vendor array support; 2 years as sustaining lead (escalations, customer-facing triage, team mentoring).
\item Java, JBoss, Tomcat, Spring, Windows, Linux, Oracle, SMI-S, Fibre Channel, iSCSI, SAN.
\end{itemize}

\resheader{EDUCATION}

\jobheading{HARVARD UNIVERSITY EXTENSION SCHOOL}{2005}{Master of Liberal Arts, Information Technology}{Cambridge, MA}

Thesis: C compiler front end and code generator---parsed Apache httpd into an IR, emitted C, and produced a build that ran as a working web server.

\end{document}
